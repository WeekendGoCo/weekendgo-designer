# WeekendGo Designer — خطة التعديلات وتتبع التقدم

> **هذا الملف هو مصدر الحقيقة الوحيد لحالة المشروع.** أي محادثة جديدة مع Claude
> يجب أن تبدأ بقراءة هذا الملف بالكامل أولاً (`view` أو `read_file`) قبل أي تعديل.
> حدّث هذا الملف بعد كل دفعة تعديلات منجزة (غيّر ⬜ إلى ✅ وأضف تفاصيل التنفيذ).
> **مهم:** لا تثق فقط بحالة ⬜/✅ القديمة دون تحقق — تأكد من الكود الفعلي دائماً
> قبل افتراض أن بنداً لم يُنفذ، لأن جلسات سابقة قد تكون نفّذت دون تحديث الملف.

**آخر تحديث:** 2026-07-14 (دفعة ثانية) — تم إنجاز: #13 السيارات بالكامل،
#5 عمود الليالي المستقل، #8 قوائم المدن المنسدلة بأربع أقسام (المعالم/المسافات/
المطاعم/التكاليف الإضافية). كما تأكدت المراجعة أن #1، #6، #7، #9، #14، #15،
#16، #17، #18، #19 كانت **منجزة فعلاً من جلسات سابقة** رغم تعليمها ⬜.
**الباقي الفعلي غير المنجز الآن فقط: #2، #3، #4، #10، #11، #12.**

**مسار المشروع:** `C:\Users\User\Downloads\weekendgo-designer`
**التقنية:** Next.js (App Router) + React + JS عادي (لا TypeScript) + JSON كقاعدة بيانات
(`hotels.json`, `countries.json`, `trips.json`, `cars.json` في جذر المشروع،
تُقرأ/تُكتب عبر `src/app/api/*/route.js`، وتُدار من الواجهة عبر `src/lib/db.js`)

**⚠️ ملاحظة تقنية مهمة لأي جلسة قادمة:** اتصال أداة الفايل سيستم (Filesystem
MCP) على جهاز المستخدم قد يتوقف عن الاستجابة أحياناً (timeout بعد عدة دقائق
حتى على عمليات قراءة بسيطة). إذا حدث هذا، أخبر المستخدم أنه يحتاج التأكد من
تشغيل/إعادة تشغيل تطبيق Claude Desktop أو الـ MCP server المحلي، ثم أعد
المحاولة — لا تفترض فشل دائم من أول محاولة.

**⚠️ لم يُشغَّل `npm run dev` بعد للتحقق البصري من أي من التعديلات في هذه
الجلسة أو السابقة.** يُرجى من المستخدم فتح المشروع محلياً واختبار، وإخبار
Claude بأي خطأ بالـ console فوراً.

---

## 0. خلفية سريعة عن بنية المشروع (لأي جلسة جديدة)

- `src/app/page.js` = **لوحة التحكم الرئيسية** (تبويبات: الباقات المحفوظة /
  إدارة مستودع الفنادق / الدول والمدن / **السيارات**). كل تبويب جديد (مثل
  الطيران) يُضاف هنا بنفس النمط.
- `src/app/designer/page.js` = **صفحة تصميم رحلة واحدة** (تجمع كل الأقسام
  الجانبية `src/components/sidebar/sections/*.jsx` مع المعاينة `src/components/preview/*.jsx`)
- `src/lib/db.js` = طبقة وصول موحدة (in-memory cache + fetch من API + fallback
  localStorage). أي مستودع بيانات جديد (مثل الطيران) يحتاج: دالة `getX` /
  `saveXList` هنا + route جديد `src/app/api/x/route.js` + ملف `x.json` بالجذر.
  **مثال جاهز يُقتدى به بالكامل: نمط `cars` المُنفَّذ حديثاً.**
- `src/lib/constants.js` = `INITIAL_TRIP_STATE` (شكل بيانات الرحلة الافتراضي)
  و `HOTEL_TIERS` (سيلفر/جولد/بلاتينيوم/رويال) و `getTierLabel()`.
- كل قسم بالمعاينة له نظيره بالـ sidebar: مثلاً `HotelsSection.jsx` (إدخال) ↔
  `HotelsBlock.jsx` (عرض/PDF).
- **نمط اختيار المدينة من قائمة مرتبطة بدولة الرحلة** (مُطبَّق الآن في
  Landmarks/Distances/Restaurants/ExtraCosts): `useState` للدول +
  `useEffect(() => setCountries(db.getCountries()), [])` + اشتقاق
  `tripCities = countries.find(c => c.name === tripData.country)?.cities || []`
  ثم استخدامها كـ `<select>` بدل `<input>` نصي. **استخدم نفس النمط لأي قسم
  مستقبلي يحتاج اختيار مدينة.**

---

## 1. حالة الطلبات (من رسالة المستخدم الطويلة الأخيرة)

### ✅ = منجز بالكامل | 🟡 = منجز جزئياً | ⬜ = لم يبدأ

| # | الطلب | الحالة | ملاحظات |
|---|---|---|---|
| 1 | تبديل مكان عنصري التواصل بالكوفر (واتساب يمين / إيميل-موقع يسار) + حذف "تواصلوا معنا" + تصحيح اتجاه/محاذاة النص | ✅ | `CoverPage.jsx` — منجز من جلسة سابقة، تأكدت بالمراجعة |
| 2 | سعر الكوفر بأسلوب تسويقي "ابتداءً من" حسب المستوى + حقول أسعار لكل مستوى بلوحة التحكم | ⬜ | لم يبدأ — `CoverPage.jsx` يعرض سعراً ثابتاً واحداً فقط (`tripData.price`). **تنفيذ مقترح:** استخدام أسعار `tripData.tiers[].price` (موجودة بالفعل من نظام Tiers) لعرض "ابتداءً من {أقل سعر}" بالكوفر بدل حقل سعر منفصل جديد |
| 3 | قسم تذاكر الطيران الكامل (مستودع مطارات + قسم ذهاب/عودة أساسي + رحلات داخلية اختياري) | 🟡 | **الأكبر.** يوجد `FlightsSection.jsx` و`FlightsBlock.jsx` لكن **لا يوجد** `src/app/api/flights` ولا `airports.json`/مستودع مطارات. **التالي:** افحص محتوى `FlightsSection.jsx`/`FlightsBlock.jsx` أولاً لمعرفة الشكل الحالي، ثم ابنِ: (أ) مستودع مطارات (مدينة-مطار-رمز) بلوحة التحكم بنفس نمط `cars`، (ب) قسم ذهاب/عودة أساسي مربوط بعدد الأشخاص المُدخل، (ج) قسم رحلات داخلية اختياري بين المدن المضافة فعلياً بالرحلة فقط (حصراً من مدن لها مطارات بالمستودع) |
| 4 | عمود "السعر مع الطيران" في ملخص الباقات بالصفحة الرئيسية | ⬜ | يعتمد على إنجاز #3 أولاً |
| 5 | عمود مستقل لعدد الليالي بجدول الفنادق (بدل الالتصاق باسم الفندق) | ✅ | **مُنجز اليوم.** `HotelsBlock.jsx` — الجدول الآن 6 أعمدة (الفندق/المدينة/وصول/مغادرة/**الليالي**/الغرف) بدل badge ملتصق باسم الفندق |
| 6 | تحسين قائمة اختيار الفندق: لون متناسق + أيقونة بدل نجمة + إظهار التصنيف (Tier) | ✅ | `HotelsSection.jsx` (قائمة اختيار فندق الرحلة) — النجمة ⭐ بأول كل عنصر بالقائمة، والتصنيف يظهر عبر `getTierLabel()`. منجز من جلسة سابقة |
| 7 | معالجة نقص فنادق مستوى معيّن بمدينة (خيار قبول مستويات أخرى مؤقتاً) | ✅ | `TiersSection.jsx` — يوجد checkbox "قبول فنادق من فئات أخرى مؤقتاً لهذه المدينة" بالضبط كما طُلب. منجز من جلسة سابقة |
| 8 | اختيار المدينة من قائمة (لا كتابة يدوية) في: المعالم، مسافات الطيران، المطاعم، التكاليف الإضافية — مرتبطة بدولة الرحلة | ✅ | **مُنجز اليوم.** حُوّلت حقول المدينة من `<input>` نصي إلى `<select>` مرتبط بـ `tripData.country` في: `LandmarksSection.jsx`, `DistancesSection.jsx` (city1 و city2), `RestaurantsSection.jsx`, `ExtraCostsSection.jsx`. مع تحذير ⚠️ إذا لم تُختر دولة/مدن للرحلة بعد |
| 9 | إصلاح خلل عرض السعر بجدول التكاليف الإضافية (يظهر أقصى اليسار بدل عموده) | ✅ | `ExtraCostsBlock.jsx` (جدول العرض بالـ PDF) — الأعمدة متطابقة بنفس `grid-template-columns`. منجز من جلسة سابقة. إن استمر الخلل فعلياً بالمتصفح أخبرني بلقطة شاشة |
| 10 | نص "شركة مرخصة من وزارة السياحة..." تحت الشعار بالفوتر بطريقة عصرية أنيقة | ⬜ | الفوتر الحالي (`FooterBlock.jsx`) يعرض السجل التجاري/الرقم الضريبي/تصريح الوزارة كأرقام بخط صغير أسفل الصفحة — ليس نفس صياغة الطلب (نص أنيق تحت الشعار مباشرة) |
| 11 | قسم بنوك/وسائل دفع بالفوتر (الراجحي، الأول، الأهلي، فيزا/ماستركارد/مدى، تابي/تمارا) بشعارات صغيرة placeholder | ⬜ | ينتظر رفع المستخدم للشعارات الرسمية بدون خلفية لاحقاً — نبني الآن مواضع نصية/أيقونات عامة placeholder فقط |
| 12 | حقل "عنوان الشركة" بلوحة التحكم (افتراضي: مكة المكرمة – شارع إبراهيم الجفالي) + عرضه بالفوتر | ⬜ | لا يوجد حقل عنوان بـ `INITIAL_TRIP_STATE` ولا بالفوتر حالياً |
| 13 | **قسم جديد "السيارات" بلوحة التحكم فقط** (نقل بسائق / إيجار يومي بدون سائق / إيجار ساعي بدون سائق، سعر يوم+ساعة، صورة، عرض بطاقات) | ✅ | **مُنجز بالكامل اليوم.** التفاصيل بالقسم 3 أدناه |
| 14 | حذف الشرطة المائلة الزائدة من رابط الموقع بالفوتر (`weekendgo.com.sa/` ← `weekendgo.com.sa`) | ✅ | `FooterBlock.jsx` و`CoverPage.jsx` — منجز من جلسة سابقة |
| 15 | سعة الغرفة (شخصين/ثلاثة) بجانب عدد الغرف + إصلاح خلل طفحان حقل "عدد الغرف" | ✅ | `HotelsSection.jsx` — حقل "سعة الغرفة" (2/3 أشخاص) موجود بجانب "الغرف" بصف مستقل خاص بهما ("own row so it never overflows the sidebar" — تعليق بالكود يؤكد إصلاح الطفحان). منجز من جلسة سابقة |
| 16 | إطار متوهج/تمييز بصري لكل مدينة ومجموعة فنادقها أثناء الإدخال (منع التداخل البصري) | ✅ | `HotelsSection.jsx` — كل مجموعة مدينة لها `border` + `boxShadow` متوهج (cyan glow) وخط فاصل يمين لقائمة فنادقها. منجز من جلسة سابقة |
| 17 | تغيير عنوان "الفنادق التي ستستقبلكم" لعنوان أدق وأعصر | ✅ | `HotelsBlock.jsx` يعرض عنوان "إقامتكم المختارة" حالياً — النص القديم المذكور بالطلب غير موجود. منجز من جلسة سابقة |
| 18 | تصفية جدول "مستويات الباقات" (TiersBlock) لعرض الفنادق المُضافة فعلياً بالرحلة فقط | ✅ | `TiersSection.jsx` — يستخدم `addedRepoIds` (فنادق الرحلة الفعلية فقط عبر `repoId`) لتصفية الخيارات المعروضة بكل مستوى/مدينة. منجز من جلسة سابقة |
| 19 | خيار "عروسين 💍" (checkbox) تحت عدد الأشخاص — إذا فُعّل تُستبدل "2 بالغين" بـ "رحلة عسل عروسين" ببطاقة الاسم | ✅ | `TripInfoSection.jsx` (حقل `isHoneymoon`) + `CoverPage.jsx`. منجز من جلسة سابقة |

### ✅ منجز من جلسات سابقة (مرجع فقط، لا تكرر):
- تصحيح دولة باتومي (نُقلت من تركيا إلى جورجيا جديدة) في `countries.json`
- تعبئة `hotels.json` بـ 29 فندق من دليل الفنادق المرفوع (5 مدن) — **ملاحظة:**
  حسب محادثة سابقة تم لاحقاً تصفير `hotels.json` كجزء من ترحيل بنية الدول/المدن
  الجديدة، تحقق من محتواه الفعلي الآن قبل افتراض وجود بيانات
- إضافة حقل `hotelUrl` (رابط الاطلاع على الفندق) في المستودع + التصميم + العرض
- اسم الفندق بالمعاينة أصبح رابطاً قابلاً للنقر لـ `hotelUrl`
- زر/أيقونة خرائط جوجل
- نظام الدول والمدن الهرمي الكامل (`countries.json` + API + تبويب CRUD)
- نظام المستويات الأربعة `HOTEL_TIERS` بـ `constants.js` + `getTierLabel()`
- نظام تحقق `tierUtils.js` (`validateTiers`, `getCityPeriods`, `getPrintableTierRows`)
  لمنع تصدير PDF عند تغطية صفرية، وإخفاء صفوف عند تغطية جزئية

---

## 2. خطة التنفيذ بالدفعات (Phases) — محدّثة

- **Phase A**: ✅ منجزة بالكامل (#1، #14، #15، #17، #19)
- **Phase B**: ⬜ #2 سعر الكوفر بالمستويات (تسويقي) — **متبقي**
- **Phase C**: ✅ منجزة بالكامل (#5، #6، #7، #16، #18)
- **Phase D**: ✅ منجزة بالكامل (#8، #9)
- **Phase E**: ⬜ #10 نص الترخيص، #11 قسم البنوك/الدفع، #12 حقل العنوان — **متبقية بالكامل**
- **Phase F**: ✅ **#13 قسم السيارات — منجز بالكامل**
- **Phase G (الأكبر)**: 🟡 #3 قسم الطيران الكامل — يوجد أساس جزئي، يحتاج فحص
  وإكمال، ثم ⬜ #4 عمود السعر مع الطيران

**التالي المقترح للجلسة القادمة (بالأولوية):**
1. Phase E السريعة نسبياً: #10 (نص ترخيص بالفوتر) + #12 (حقل عنوان الشركة) —
   عمل سريع مباشر. #11 (شعارات الدفع) ينتظر رفع المستخدم للشعارات.
2. Phase B: #2 سعر الكوفر التسويقي "ابتداءً من" — مرتبط بنظام Tiers الموجود.
3. Phase G: #3/#4 الطيران — الأكبر، يبدأ بفحص `FlightsSection.jsx`/`FlightsBlock.jsx`
   الحاليين لتحديد نقطة البداية الفعلية.

---

## 3. سجل التنفيذ التفصيلي (يُحدّث أولاً بأول، الأحدث بالأعلى)

### 2026-07-14 (دفعة ثانية) — #5 عمود الليالي + #8 قوائم المدن المنسدلة
**#5 — `src/components/preview/HotelsBlock.jsx`:**
- تغيير `grid-template-columns` لجدول الملخص من 5 أعمدة (`2fr 1.2fr 1.2fr 1.2fr 0.7fr`)
  إلى 6 أعمدة (`2fr 1fr 1fr 1fr 0.7fr 0.7fr`) لإضافة عمود "الليالي" مستقل
- إزالة `hotel-nights-badge` الملتصق باسم الفندق، واستبداله بخلية `.hotel-table-nights` مستقلة بعمودها الخاص

**#8 — أربعة ملفات بـ `src/components/sidebar/sections/`:**
- `LandmarksSection.jsx`, `RestaurantsSection.jsx`: حقل مدينة المجموعة تحوّل
  من `<input>` نصي إلى `<select>` مبني من `db.getCountries()` مفلترة على
  `tripData.country`
- `DistancesSection.jsx`: نفس التحويل لحقلي `city1` و `city2` معاً
- `ExtraCostsSection.jsx`: نفس التحويل لحقل `city` بكل بند
- الأربعة يعرضون تحذيراً ⚠️ إن لم تُختر دولة/مدن للرحلة بعد من قسم المعلومات الأساسية

### 2026-07-14 (دفعة أولى) — قسم السيارات (البند #13) ✅
**الملفات المُنشأة:**
- `cars.json` (جذر المشروع) — مصفوفة فارغة `[]` عند الإنشاء، تُملأ من لوحة التحكم
- `src/app/api/cars/route.js` — GET (قراءة) / POST (حفظ) بنفس نمط `api/hotels/route.js`

**الملفات المُعدَّلة:**
- `src/lib/db.js` — إضافة `carsCache`، تحميله ضمن `db.init()` مع fallback
  لـ localStorage (`weekendgo_cars_db`)، تصفيره ضمن `db.reset()`، ودالتا
  `db.getCars()` / `db.saveCarsList(carsList)`
- `src/app/page.js` — إضافة:
  - استيراد أيقونات `Car, Clock, CalendarDays, UserCheck` من `lucide-react`
  - state جديد `cars` + تحميله بـ `useEffect` الابتدائي
  - handlers: `handleAddCar`, `handleUpdateCar`, `handleUpdateCarService`,
    `handleCarImageUpload`, `handleDeleteCar`
  - تبويب جديد "السيارات (عدد)" بجانب تبويب "الدول والمدن"
  - محتوى التبويب: شبكة بطاقات، كل بطاقة سيارة تحتوي: صورة (رفع base64)،
    اسم/موديل، فئة نصية اختيارية، قسم "نقل مع سائق" (checkbox تفعيل + سعر
    اليوم)، قسم "إيجار بدون سائق" (checkbox تفعيل + سعر اليوم + سعر الساعة)

**شكل بيانات السيارة الواحدة:**
```json
{
  "id": "string",
  "name": "اسم/موديل السيارة",
  "category": "نص اختياري (اقتصادية/عائلية/فاخرة/فان)",
  "image": "base64",
  "withDriver": { "enabled": true, "dailyPrice": "" },
  "selfDrive": { "enabled": false, "dailyPrice": "", "hourlyPrice": "" }
}
```

**قرار مؤكد من المستخدم:** لوحة التحكم فقط حالياً، بدون دمج بالـ PDF/تصميم
الرحلة. لا تُضف قسم سيارات لـ `INITIAL_TRIP_STATE` أو مكونات `HotelsBlock`
المماثلة إلا إذا طلب المستخدم ذلك صراحة لاحقاً.

### مراجعة شاملة 2026-07-14
تمت مراجعة الكود الفعلي لكل البنود الـ19 فتبيّن أن 10 بنود كانت منجزة فعلياً
من جلسات سابقة دون تحديث هذا الملف: #1، #6، #7، #9، #14، #15، #16، #17، #18، #19.
**الدرس المستفاد:** لا تعتمد فقط على حالة الجدول هنا — تحقق من الكود دائماً.

---

## 4. ملاحظات وقرارات مهمة يجب تذكرها

- مستودع السيارات (#13): قرار المستخدم = **لوحة التحكم فقط حالياً**، بدون دمج
  بالـ PDF/تصميم الرحلة. لا تضف قسم سيارات لـ `INITIAL_TRIP_STATE` أو
  `HotelsBlock`-مثيلاتها إلا إذا طلب المستخدم ذلك صراحة لاحقاً.
- شعارات البنوك/الدفع (#11): المستخدم سيرفعها لاحقاً بصيغة بدون خلفية. اعمل
  الآن مواضع/placeholders فقط بنص أو أيقونة عامة مؤقتة، ولا تخترع شعارات.
- تصنيفات VIP بملف الفنادق الأصلي (VIP1..5, Super VIP) تم تقريبها يدوياً إلى
  4 مستويات النظام (silver/gold/platinum/royal) — إذا طلب المستخدم دقة أكبر
  راجع `hotels_guide_final.md` الأصلي (كان مرفوعاً بمحادثة سابقة، غير محفوظ
  بالمشروع، اطلبه من المستخدم إذا احتجته).
- جدول `HOTEL_TIERS` موجود في `src/lib/constants.js` — أي شاشة تعرض "تصنيف
  الفندق" يجب أن تستخدم `getTierLabel()` الموجودة هناك، لا نصوص يدوية.
- **قبل الشروع بأي بند جديد:** افحص الملفات ذات العلاقة فعلياً (`view`/`read_file`)
  بدل افتراض حالتها من هذا الجدول فقط.
- **نمط اختيار المدينة المرتبط بالدولة** (مُطبَّق بالبند #8): يُعاد استخدامه
  لأي حقل مدينة مستقبلي (بما فيها مستودع مطارات الطيران #3).
- إذا توقفت أدوات الفايل سيستم عن الاستجابة (timeout)، أخبر المستخدم أنه يحتاج
  إعادة تشغيل الـ MCP server / تطبيق Claude Desktop المحلي، ثم أعد المحاولة.
- **لم يُشغَّل `npm run dev` بعد للتحقق البصري** من أي تعديل بهذه الجلسة أو
  السابقة — كل الأعمال أُنجزت عبر تعديل الكود مباشرة دون تشغيل الخادم المحلي
  للتحقق. اطلب من المستخدم الاختبار وإبلاغك بأي أخطاء console.
